<%-include('partials/header')%>

<div class="row mt-4">

    <% if(user.id==user_id){%>
        <!-- hello -->
    <div class="d-flex justify-content-end">
        <a href="/edit-post/<%=post._id%>"><i class="bi bi-pencil-square text-primary"></i></a>
        <a href=""><i class="bi bi-trash3-fill mx-2 text-danger"></i></a>
    </div>
    <%}%>

    <h4><%=post.title%></h4>
    <h6>Posted by <%=post.user.username%> on <%=post.createdAt%></h6>
    <p><%=post.body%></p>

</div>



<%-include('partials/footer')%>